# Database
DATABASE_URL=mysql://root:password@localhost:3306/expense_app

# Auth
JWT_SECRET=change-me-to-a-long-random-string

# Server
PORT=5000

# Currency — default USD→INR rate when user does not specify one at import/balance time
USD_TO_INR_RATE=83
